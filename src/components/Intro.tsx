import { Typography } from "@mui/material";

const Intro = () => {
  return (
    <>
      <Typography variant="h4">
        Welcome to Dan's cash-conversion app!
      </Typography>
      <Typography>
        Simply select two currencies from the provided dropdowns, and enter an
        amount to convert. Then press the convert button!
      </Typography>
    </>
  );
};

export default Intro;
